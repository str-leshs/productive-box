name: Update gist
on:
  repository_dispatch:
    types: [build-event]
  schedule:
    - cron: "0 0 * * *"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.gist:b3f377ad26b163acc33cd322a7af44d8}}
          TIMEZONE: ${{ vars.Asia/Seoul }}
